import { defineConfig } from 'vite'

// Set the `base` to the repository name so GitHub Pages (project page)
// serves built assets from the correct path.
export default defineConfig({
  base: '/website-for-finding-love/'
})
